describe('Users (e2e)', () => {
  it.todo('/api/v1/users/me returns 401 without token');
  it.todo('/api/v1/users/me returns safe profile with valid token');
  it.todo('/api/v1/users/me response never includes passwordHash');
});
